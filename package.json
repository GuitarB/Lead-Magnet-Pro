{
  "name": "lead-magnet-pro",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered lead magnet generator deployed on Cloudflare Pages",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy ."
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.10"
  }
}
